{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Post</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enviar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('post_index') }}">Regresar a la lista</a>
        </li>
    </ul>
{% endblock %}
